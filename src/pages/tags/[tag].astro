---
import Layout from '../../layouts/Layout.astro';
import "@/styles/tag.css";
import {
  CONTENT_MAP
} from "../../utils/getMarkdownContent";

/*export async function getStaticPaths() {
  const allTags = getSortedTermsByType(allPosts, "tags");
  return Array.from(allTags).map((tag: string) => {
    const tagSlug = tag.toLowerCase().replace(/[\/\s]+/g, '-');
    const postsInTag = allPosts.filter((post: any) =>
      post.frontmatter.tags && post.frontmatter.tags.includes(tag)
    ).sort((a: any, b: any) => new Date(b.frontmatter.date).getTime() - new Date(a.frontmatter.date).getTime());

    return {
      params: { tag: tagSlug },
      props: {
        tag,
        posts: postsInTag
      }
    };
  });
}*/

export async function getStaticPaths() {
  return Object.entries(CONTENT_MAP).flatMap(([collection, data]) =>
      data.tags.map(([tag, posts]) => {
        const tagSlug = tag.toLowerCase().replace(/[\/\s]+/g, '-');
        return {
          params: { tag: tagSlug },
          props: {
            tag,
            posts,
            collection, // ✅ truyền thêm collection
          },
        };
      })
  );
}

const { tag, posts, collection } = Astro.props;
const base = import.meta.env.BASE_URL;
const title = "Tags";
const path = `/${collection}/tags`; // ✅ đường dẫn theo collection
const description = `Tất cả bài viết về ${tag}`;
---

<Layout title={tag} description={description}>
  <div class="tag-page">
    <header class="page-header">
      <h1 class="page-title">Tag: {tag}</h1>
      <p class="page-description">
        {posts.length} bài viết trong collection <strong>{collection}</strong>
      </p>
      <hr class="page-rule" />
    </header>

    <div class="posts-list">
      {posts.map((post: any) => {
        const slug = post.file.split('/').pop()?.replace(/\.(md|mdx)$/, '');
        return (
            <article class="post-item">
              <h2 class="post-title">
                <a href={`${base}${post.frontmatter.type}/${slug}`}>
                  {post.frontmatter.title}
                </a>
              </h2>

              <div class="post-meta">
                <time
                    class="post-date"
                    datetime={new Date(post.frontmatter.date).toISOString()}
                >
                  {new Date(post.frontmatter.date).toLocaleDateString("vi-VN", {
                    year: "numeric",
                    month: "long",
                    day: "numeric"
                  })}
                </time>
              </div>

              {post.frontmatter.excerpt && (
                  <p class="post-excerpt">{post.frontmatter.excerpt}</p>
              )}

              <a href={`${base}${post.frontmatter.type}/${slug}`} class="read-more">
                Đọc tiếp →
              </a>
            </article>
        );
      })}
    </div>

    <div class="page-navigation">
      <a href={`${base}${collection}/tags`} class="back-link">
        ← Xem tất cả Tags trong {collection}
      </a>
    </div>
  </div>
</Layout>
