---
const siteName = "Tổ Sư Thiền";
---

<section class="pwa-install" id="pwa-install" data-hide-in-pwa>
    <h2  id="pwa-installation-title">📲 Cài Tổ Sư Thiền ra màn hình điện thoại</h2>
    <p class="subtitle">
        Mở nhanh – Dùng như app – Không cần cài đặt từ <b class="green">Google Play</b> / <b class="red">App Store</b>
    </p>

    <div class="platforms">
        <!-- ANDROID -->
        <div class="platform android" data-platform="android">
            <h3 class="green">
                <img src="/icons/android-icon.png" style="display: inline; height: 16px; margin-inline: 4px" alt="android icon"/>
                ANDROID
            </h3>
            <ol>
                <li>Bấm dấu <strong>⋮</strong> (3 chấm đứng)</li>
                <li>Chọn <strong>Thêm vào Màn hình chính (Add to Homepage)</strong></li>
                <li>Bấm <strong>Thêm (Add)</strong></li>
            </ol>
        </div>

        <!-- IOS -->
        <div class="platform ios" data-platform="ios">
            <h3 class="red">
                <img src="/icons/ios-icon.png" style="display: inline; height: 16px; margin-inline: 4px" alt="ios icon"/>
                iOS
            </h3>
            <ol>
                <li>Bấm nút <strong>Chia sẻ (Share)</strong> 📤</li>
                <li>Chọn <strong>Thêm vào Màn hình chính (Add to Homepage)</strong></li>
                <li>Bấm <strong>Thêm (Add)</strong></li>
            </ol>
            <p class="note">
                ⚠️ Phải mở bằng trình duyệt <b>Safari</b>
                <img src="/icons/safari-icon.png" style="display: inline; height: 16px; margin-inline: 4px" alt="safari icon"/>
            </p>
        </div>
    </div>
</section>

<style>
    .pwa-install {
        background: #fff8e1;
        border-radius: 16px;
        padding: 16px;
        margin: 12px 0;
        font-size: 12px;
    }

    .pwa-install h2 {
        font-size: 18px;
        margin-bottom: 8px;
    }

    .subtitle {
        font-size: 18px;
        color: var(--color-accent-orange);
        margin-bottom: 12px;
    }

    .platforms {
        display: grid;
        grid-template-columns: 1fr;
        gap: 20px;
    }

    .platform {
        background: white;
        border-radius: 12px;
        padding: 8px;
    }

    .platform h3 {
        font-size: 16px;
        margin-bottom: 4px;
    }

    .green {
        color: var(--color-accent-green);
    }

    .red {
        color: var(--color-accent-red);
    }

    .platform ol {
        padding-left: 20px;
    }

    .platform li {
        margin-bottom: 10px;
        line-height: 1.4;
        color: var(--color-accent-indigo);
    }

    .note {
        font-size: 18px;
        color: var(--color-accent-red);
        margin-top: 8px;
    }

    @media (min-width: 768px) {
        .platforms {
            grid-template-columns: 1fr 1fr;
        }
    }
    /* Nếu đang ở PWA → ẩn */
    @media (display-mode: standalone) {
        [data-hide-in-pwa] {
            display: none !important;
        }
    }
</style>
