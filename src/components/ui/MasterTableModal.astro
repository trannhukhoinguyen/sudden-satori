---
import AllMasters from '@/data/masters/AllMasterDb';
import type { Master } from "@/types";
import MasterTable from '@/components/ui/MasterTable.astro';

type CustomizeMaster = Pick<
    Master,
    | 'name_vi'
    | 'nickname_vi'
    | 'name_en'
    | 'name_zh'
    | 'name_jp'
    | 'sect'
    | 'country'
    | 'teachers'
    | 'epoch'
    | 'hasContent'
    | 'references'
    | 'wiki'
>;

const customizeAllMasters: CustomizeMaster[] =
    AllMasters
        ?.map(({
                 sect,
                 hasContent,
                 references,
                 wiki,
                 country,
                 teachers,
                 epoch,
                 nickname_vi,
                 name_vi,
                 name_en,
                 name_zh,
                 name_jp,
               }: Master) => ({
              name_vi,
              nickname_vi,
              name_en,
              name_zh,
              name_jp,
              sect,
              country,
              teachers,
              epoch,
              hasContent,
              references,
              wiki,
            })
        );
const headRows = [
    'Tên Việt',
    'Tên Anh',
    'Tên Trung',
    'Tên Nhật',
    'Tông phái',
    'Quốc tịch',
    'Sư phụ',
    'Thời đại',
    'Link',
];
---

<style>
  @keyframes pulse {
    0% { opacity: 1 }
    50% { opacity: 0.4 }
    100% { opacity: 1 }
  }

  .content-icon {
    width: auto;
    height: 1.1rem;
    color: var(--color-accent-gold);
    cursor: pointer;
    margin-left: 0.5rem;
    transition: transform 0.2s;


    animation: pulse 3s infinite;
  }
  .content-icon:hover {
    transform: scale(1.3);
  }

  /* Modal overlay */
  .content-modal-overlay {
    position: fixed;
    inset: 0;
    background: var(--color-background-overlay), var(--color-black) ;
    display: flex;
    justify-content: center;
    align-items: center;
    z-index: 1000;
  }

  /* Modal content */
  .content-modal {
    background: var(--color-background), var(--color-white);
    border-radius: 8px;
    width: 90%;
    max-width: 900px;
    height: 90%;
    display: flex;
    flex-direction: column;
  }

  .content-modal-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 0.5rem 1rem;
    background: var(--color-background), var(--color-white);
    border-bottom: 1px solid var(--color-border);
  }

  .content-modal-header button {
    background: var(--color-accent-gold-dark);
    color: var(--color-text-primary);
    border: none;
    border-radius: 4px;
    padding: 0.25rem 0.5rem;
    cursor: pointer;
  }

  .content-modal iframe {
    flex: 1;
    border: none;
  }
</style>

<!-- Trigger icon -->
<span style="font-size: 2rem; float: right" class="content-icon" title="Xem DANH SÁCH THIỀN SƯ">🔎</span>

<!-- Modal -->
<div class="content-modal-overlay" id="content-modal" style="display:none;">
  <div class="content-modal">
    <div class="content-modal-header">
      <h2 style="float: right">DANH SÁCH THIỀN SƯ</h2>
      <div>
        <button id="content-close">✖</button>
      </div>
    </div>
    <div>
      <MasterTable data={ customizeAllMasters } headRows={ headRows } />
    </div>
  </div>
</div>

<script>
  const trigger = document.getElementById("content-trigger");
  const modal = document.getElementById("content-modal");
  const closeBtn = document.getElementById("content-close");

  trigger?.addEventListener("click", () => {
    modal.style.display = "flex";
  });

  closeBtn?.addEventListener("click", () => {
    modal.style.display = "none";
  });

  modal?.addEventListener("click", (e) => {
    if (e.target === modal) modal.style.display = "none"; // click outside closes
  });
</script>
