---
const {
    title,
    explainTitle,
} = Astro.props;
---

<fieldset style="margin-block: 8px">
    <legend><h4>⚡️ {title}</h4></legend>
    <div style="color: var(--color-accent-darkorange)">
        <slot />
    </div>
</fieldset>
{
  explainTitle ? <h5>💥 { explainTitle }</h5> : null
}


