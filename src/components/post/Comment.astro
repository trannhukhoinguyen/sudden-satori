---
const { id, children } = Astro.props;
---

<div
  id={`comment-${id}`}
  class="hidden mt-6 p-4 border-l-4 border-emerald-500 bg-emerald-50 dark:bg-slate-900 dark:border-emerald-700"
>
  <h3 class="text-lg font-semibold mb-2">BÌNH XƯỚNG</h3>
  <slot />
</div>
