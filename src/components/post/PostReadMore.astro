---
const { collection, file } = Astro.props;
const base = import.meta.env.BASE_URL;
---

<div class="post-navigation">
    <a
        href={`${base}${collection}/${file.split('/').pop()?.replace(/\.(md|mdx)$/, '')}`}
        class="read-more"
    >
        Xem thêm →
    </a>
</div>
